# Copy this file to .env and fill in values as needed.
# These variables enable real AI image generation in LyricistAgent.
# If left unset, the app will fall back to a local Pillow placeholder.

# Replicate API (Stable Diffusion / SDXL etc.)
# Obtain a token from https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=

# Optional: choose a specific model slug (default: stability-ai/sdxl)
REPLICATE_MODEL=stability-ai/sdxl

# OpenAI Images API (DALL·E)
# Obtain a key from https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional: override image model (default: gpt-image-1 or dall-e-3 depending on SDK)
OPENAI_IMAGE_MODEL=gpt-image-1

# Optional common size for image generation providers (format: WxH)
IMAGE_SIZE=1024x1024
